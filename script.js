


const userName = prompt("Enter your name: ");
const userPassword = prompt("Enter your password: ");

if (userName === "Bilal" && userPassword === "BMJ") {
  alert("Login successful!");
} else {
  alert("Invalid username or password!");
}